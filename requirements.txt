requests>=2.31
beautifulsoup4>=4.12
Jinja2>=3.1
PyYAML>=6.0
# Alleen nodig als je Postgres/Supabase gebruikt in plaats van een lokaal bestand.
psycopg[binary]>=3.1
# Nederlandse tijd in het dashboard, ook zonder tijdzonedatabase op de server.
tzdata>=2024.1
# Alleen nodig als je per bedrijf eigen teksten wilt laten schrijven (LM_AI_TEKST).
anthropic>=1,<2
